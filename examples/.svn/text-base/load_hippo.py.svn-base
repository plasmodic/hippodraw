""" -*- mode: python -*-

This script allows one to run multiple scripts from on script because
it ensures that only one application instance is created.

"""

import hippo
app = hippo.HDApp()
canvas = app.canvas ()

