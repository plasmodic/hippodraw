#include "axes/AxisModelLinear.h"
#include "axes/Range.h"
  
int main( int argc, char * argv )
{
  /*  XAxisModelLinear * axis, * axis2;
      axis = new AxisModelLinear();
      
      axis->setAll( *( new Range( 0, 100 ) ), 10 );
      cout << "----> in test program\n";
      cout << "Back from the setAll\n";
      cout << "Range : min = " << axis->getMin()
      << " , max = " << axis->getMax() << "\n";
      cout << "First tick = " << axis->getFirstTick() << "\n";
      cout << "magnitude = " << axis->getMagnitude() << "\n";
      cout << "tick step = " << axis->getTickStep() << "\n";
      
      cout << "\n-----------------------------------------\n\n";
      
      axis2 = new XAxisModelLinear();
      
      axis2->setAll( *( new Range( 0.3, 99.5 ) ), 10 );
      cout << "----> in test program\n";
      cout << "Back from the setAll\n";
      cout << "Range : min = " << axis->getMin()
      << "max = , " << axis->getMax() << "\n";
      cout << "First tick = " << axis->getFirstTick() << "\n";
      cout << "magnitude = " << axis->getMagnitude() << "\n";
      cout << "tick step = " << axis->getTickStep() << "\n";
  */
  return 1;
}

